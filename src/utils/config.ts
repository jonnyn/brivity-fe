const url = "https://brivity-react-exercise.herokuapp.com";

export const apiEndpoints = {
  base: `${url}/`,
  create_user: `${url}/users`,
  sign_in: `${url}/users/sign_in`,
  posts: `${url}/posts`,
  comment: `${url}/comment`,
  comments: `${url}/comments`,
};
